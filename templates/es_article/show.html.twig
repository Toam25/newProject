{% extends 'base.html.twig' %}

{% block title %}EsArticle{% endblock %}

{% block body %}
    <h1>EsArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ es_article.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ es_article.image }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ es_article.type }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ es_article.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('es_article_index') }}">back to list</a>

    <a href="{{ path('es_article_edit', {'id': es_article.id}) }}">edit</a>

    {{ include('es_article/_delete_form.html.twig') }}
{% endblock %}
