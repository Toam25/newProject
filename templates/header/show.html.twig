{% extends 'base.html.twig' %}

{% block title %}Header{% endblock %}

{% block body %}
    <h1>Header</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ header.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ header.name }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ header.createdAt ? header.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('header_index') }}">back to list</a>

    <a href="{{ path('header_edit', {'id': header.id}) }}">edit</a>

    {{ include('header/_delete_form.html.twig') }}
{% endblock %}
